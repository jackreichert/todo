type Mutation {
  addTask(title: String!, status: Boolean!): Task
  updateTaskStatus(id: ID!, status: Boolean!): Task
  deleteTask(id: ID!): Boolean
}

type Task {
  id: ID!
  title: String!
  status: Boolean!
}

type Query {
  getTasks: [Task]
}
