type Mutation {
  addTask(title: String!, status: Boolean!): Task
  updateTaskStatus(id: ID!, status: Boolean!): Task
}

type Task {
  id: ID!
  title: String!
  status: Boolean!
}

type Query {
  getTasks: [Task]
}
